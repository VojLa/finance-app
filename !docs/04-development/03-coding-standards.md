# Coding Standards

- Keep domain logic out of React components.
- Keep Next.js API routes thin during migration.
- Prefer explicit domain services and repositories in Python.
- Use Rust for deterministic heavy calculations.
- Preserve account currency semantics; do not hard-code CZK for account snapshots.

