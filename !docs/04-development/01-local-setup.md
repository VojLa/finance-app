# Local Setup

Common local services:

- Next.js frontend/backend adapter
- PostgreSQL
- Python FastAPI backend on port `8010`
- Rust calculation workspace

Useful commands:

```powershell
npm run dev -- -p 3010
npm run docker:api
npm run test
```
