# Testing

Current test layers:

- TypeScript checks: `npx.cmd tsc --noEmit`
- Frontend/backend TypeScript tests: `npm.cmd test`
- Lint: `npm.cmd run lint`
- Rust tests: `cargo test` in `backend/rust`
- Python tests once Python is available in PATH

